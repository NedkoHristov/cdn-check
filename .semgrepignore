# Semgrep ignore patterns

# Docker compose security warnings - intentional design decisions
docker-compose.yml

# app.py intentional security decisions:
# - SSL verification disabled to check sites with bad certificates (core functionality)
# - debug=True is fine for local development (never used in production - we use gunicorn)
app.py

# static/js/app.js - innerHTML is safe because we use escapeHtml() for sanitization
static/js/app.js
